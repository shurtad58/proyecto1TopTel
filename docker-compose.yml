version: "2"
services:
  nodeapp:
    container_name: nodeapp
    restart: always
    build: .
    ports:
      - "80:3000"
    links:
      - mongo-server      
  mongo-server:
    container_name: mongo-server
    image: mongo
    ports:
      - "27017:27017"